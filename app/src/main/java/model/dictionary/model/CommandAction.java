package model.dictionary.model;


public class CommandAction extends BaseAction {
    public CommandAction(ActionType actionType, ExecutePlaceType executePlace){
        super(actionType, executePlace);
    }
}
